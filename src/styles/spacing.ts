/**
 * Единая система отступов — 15 ролей.
 *
 * Принципы:
 * - 8px grid (все значения кратны 4)
 * - 3 уровня в каждой категории: sm / md / lg
 * - Последовательность > точность до пикселя
 */
export const s = {
  /** Вертикальные отступы секций — основные */
  section: 'py-20 md:py-32',

  /** Вертикальные отступы секций — компактные */
  sectionCompact: 'py-8 md:py-10',

  /** Горизонтальные отступы контейнера */
  container: 'px-4 md:px-8',

  /** Горизонтальные отступы контейнера — широкие (header) */
  containerWide: 'px-4 md:px-6',

  /** Отступ после мелкого элемента (тег, заголовок) */
  mbSm: 'mb-4',

  /** Отступ между блоками контента */
  mbMd: 'mb-8',

  /** Отступ для крупных блоков (цитаты, гриды) */
  mbLg: 'mb-12',

  /** Gap — мелкий (кнопки, иконки) */
  gapSm: 'gap-3',

  /** Gap — стандартный (карточки, flex-ряды) */
  gapMd: 'gap-4',

  /** Gap — большой (крупные группы) */
  gapLg: 'gap-8',

  /** Вертикальный стек — стандартный */
  stack: 'space-y-4',

  /** Внутренний отступ — мелкий */
  cardSm: 'p-4',

  /** Внутренний отступ — стандартный (карточки) */
  card: 'p-5',

  /** Внутренний отступ — крупный (большие карточки) */
  cardLg: 'p-6 md:p-8',

  /** Divider — вертикальные отступы */
  divider: 'my-8 md:my-12',
} as const;
